program programa; { Isso aqui é um comentário e deve ser ignorado }
Integer NUM;
{
Isso aqui também é
um comentário
e também deve ser ignorado
}
begin
begin
  { {{{ }
   num := ((1 + 2) + (3 + 4)) + ((5 + 6) + (7 + 8));
end;
end.
