PROGRAM programa;
REAL x, y;
BEGIN
BEGIN
  x := 32;
  y := 64;

   repeat
      begin
         x := x - 1;
         y := (8 + 90) * (x);
         x := x * y;
      end;
   until ((x <= y) OR (x = 7));
END;
END.
